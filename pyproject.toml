[tool.bandit]

[tool.black]
line-length = 120
target-version = ['py310']
skip-string-normalization = true

[tool.isort]
profile = 'black'

[tool.mypy]
python_version = '3.10'
warn_return_any = true
warn_unused_configs = true
